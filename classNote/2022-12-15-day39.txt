◆시험 풀이 [데이터베이스 구현 - 문제해결시나리오]
1번 문제
- MySQL에서는 계정 생성을 오직 root 계정 곧 관리자만 계정을 생성할 수가 있다!


 [데이터베이스 구현 - 서술형]
6번 문제. PRIMARY 제약 조건
- 컬럼 값에 NULL과 중복을 허용하지 않음으로써 컬럼 안에서 고유 식별자의 역할을 하도록 한다.


8번 문제
- PRIMARY KEY : 테이블에서 한 행의 정보를 식별하기 위해 사용할 컬럼으로 NOT NULL과 UNIQUE제약조건이 같이 들어가있는 것을 의미함(컬럼레벨과 테이블레벨로 지정)
- UNIQUE : 컬럼에 입력값에 대해서 중복을 제한하는 제약조건(컬럼레벨과 테이블레벨로 지정)
- NOT NULL : 해당컬럼에 반드시 값이 기록되어야 하는 경우(컬럼레벨로만 지정)


전제조건 : 한 학생이 계절학기를 하나의 강좌만 들을 수 있다는 
정규화 : 중복된 데이터의 테이블을 나눠주는 것
반(비)정규화 
- 데이터의 중복을 허용 => JOIN 횟수를 줄여서 성능을 향상시킴
- 정규화가 진행될수록 테이블이 많아짐
=> 원하는 데이터를 조회하기 위해서 JOIN을 많이 하게 됨
=> 데이터가 많은 경우 JOIN할 때 속도가 느려짐.

◆CGV 영화 예매 사이트 구축을 위한 데이터베이스 설계를 해보자.
- 필요하면 제한 조건을 두어서 설계
